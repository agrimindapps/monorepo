import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF4CAF50); // Verde para veterinária
  static const primaryLight = Color(0xFF80CBC4);
  static const primaryDark = Color(0xFF2E7D32);
  
  // Secondary colors
  static const secondary = Color(0xFF03DAC6);
  static const secondaryLight = Color(0xFF66FFF6);
  static const secondaryDark = Color(0xFF00A896);
  
  // Neutral colors
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const onSurface = Color(0xFF1F1F1F);
  static const onBackground = Color(0xFF1F1F1F);
  
  // Semantic colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFF9800);
  static const error = Color(0xFFF44336);
  static const info = Color(0xFF2196F3);
  
  // Text colors
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textDisabled = Color(0xFFBDBDBD);
  
  // Divider
  static const divider = Color(0xFFE0E0E0);
}