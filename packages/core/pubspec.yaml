name: core
description: Services de infraestrutura compartilhados - Firebase, RevenueCat, Drift
version: 1.0.0

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase Stack
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  firebase_analytics: ^12.0.0
  firebase_crashlytics: ^5.0.0
  firebase_storage: ^13.0.0
  cloud_firestore: ^6.0.0
  cloud_functions: ^6.0.0
  firebase_remote_config: ^6.0.0
  firebase_messaging: ^16.0.0
  
  # RevenueCat para assinaturas
  purchases_flutter: ^9.2.0
  
  # Drift para storage local SQL
  # sqlite3_flutter_libs: ^0.5.24
  
  # SharedPreferences para cache cross-module
  shared_preferences: ^2.4.0
  
  # Conectividade
  connectivity_plus: ^6.1.5
  
  # HTTP Client
  dio: ^5.9.0
  
  # Formatação e internacionalização
  intl: '>=0.19.0 <0.21.0'
  
  # Sistema de arquivos
  path_provider: ^2.1.5
  
  # App Rating
  rate_my_app: 2.2.0
  url_launcher: ^6.3.2
  
  # Local Notifications
  flutter_local_notifications: ^19.4.0
  permission_handler: ^12.0.1
  timezone: ^0.10.1

  # Text-to-Speech (Accessibility)
  flutter_tts: ^4.1.0
  
  # Security Services
  crypto: ^3.0.6
  encrypt: ^5.0.1
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.2.4
  
  # Social Authentication
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.2
  flutter_facebook_auth: ^6.0.4
  
  # File Management
  path: ^1.9.1
  mime: ^2.0.0
  archive: ^4.0.7

  # Performance Monitoring
  device_info_plus: ^12.3.0
  firebase_performance: ^0.11.1
  package_info_plus: ^9.0.0
  
  # Image Handling
  image_picker: ^1.2.1
  cached_network_image: ^3.4.1
  image: ^4.7.2

  # State Management & Utils
  flutter_bloc: ^9.1.1
  dartz: ^0.10.1
  equatable: ^2.0.7
  rxdart: ^0.28.0
  
  # Riverpod State Management
  riverpod: ^3.0.3
  flutter_riverpod: ^3.0.3
  riverpod_annotation: ^3.0.3

  # Supabase
  supabase_flutter: ^2.9.1

  # Google Mobile Ads
  google_mobile_ads: ^6.0.0

  # Navigation
  go_router: ^16.1.0

  # UI Components
  cupertino_icons: ^1.0.8
  flutter_staggered_grid_view: ^0.7.0
  shimmer: ^3.0.0
  skeletonizer: ^2.1.1

  # Icons and Sharing
  font_awesome_flutter: ^10.7.0
  icons_plus: ^5.0.0
  share_plus: ^12.0.0

  # UI Widgets
  carousel_slider: ^5.1.1
  
  # JSON Serialization
  json_annotation: ^4.9.0

  # UUID Generation
  uuid: ^4.5.1

  # Code Generation - Immutability
  freezed_annotation: ^3.1.0

  # Logging
  logger: ^2.6.2

  # Analysis/Linting (needed for transitive analysis_options.yaml inheritance)
  flutter_lints: ^5.0.0

  # Drift - SQL Database (Production-ready)
  drift: ^2.30.1
  sqlite3_flutter_libs: ^0.5.41
  sqlite3_web: ^0.4.1  # Web support with WASM
  
  web: any
  fl_chart: ^1.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Code Generation
  build_runner: ^2.4.13
  json_serializable: ^6.8.0
  freezed: ^3.0.0
  drift_dev: ^2.28.0
  riverpod_generator: ^3.0.3
  
  # Testing
  mockito: ^5.4.4
  bloc_test: ^10.0.0

flutter:
