name: app_plantis
description: CantinhoVerde - Seu jardim de apartamento sempre vivo
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Core Package - Serviços compartilhados (Firebase, Riverpod, dartz, equatable, etc.)
  core:
    path: ../../packages/core



  # Drift - SQL Database
  google_fonts: ^6.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  build_runner: ^2.4.13
  drift_dev: ^2.28.0
  riverpod_generator: ^3.0.3

flutter:
  uses-material-design: true

  assets:
    - web/sqlite3.wasm
    - web/drift_worker.dart.js
