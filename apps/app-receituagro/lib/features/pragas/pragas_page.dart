import 'package:flutter/material.dart';
import 'home_pragas_page.dart';

class PragasPage extends StatelessWidget {
  const PragasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const HomePragasPage();
  }
}