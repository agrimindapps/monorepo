name: app_agrihurbi
description: "Sistema completo de gestão agropecuária com calculadoras especializadas"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # Core Package - Serviços compartilhados (inclui Riverpod, dartz, equatable, shared_preferences)
  core:
    path: ../../packages/core

  # Dependency Injection
  get_it: ^8.2.0
  injectable: ^2.5.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  injectable_generator: ^2.6.2
  
flutter:
  uses-material-design: true
