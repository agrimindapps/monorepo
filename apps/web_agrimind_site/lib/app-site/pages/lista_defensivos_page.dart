import 'package:flutter/material.dart';

class DefensivosListarPage extends StatelessWidget {
  const DefensivosListarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Defensivos Agrícolas')),
      body: const Center(child: Text('Página em construção')),
    );
  }
}
