// WRAPPER PAGE - Mantém compatibilidade total com navegação existente
// Implementação refatorada está em: subscription/presentation/pages/subscription_clean_page.dart

import 'package:flutter/material.dart';

import 'presentation/pages/subscription_clean_page.dart';

class SubscriptionPage extends StatelessWidget {
  const SubscriptionPage({super.key});

  @override
  Widget build(BuildContext context) {
    // Redirecionamento transparente para implementação refatorada
    // Mantém 100% de compatibilidade com todas as referências existentes
    return const SubscriptionCleanPage();
  }
}