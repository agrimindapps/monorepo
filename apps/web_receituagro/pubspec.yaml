name: receituagro_web
description: "Receituagro Web Application - Agricultural diagnosis and recommendations"

publish_to: 'none'

version: 2024.08.05+2

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Core Package
  core:
    path: ../../packages/core
  
  # UI Components
  cupertino_icons: any
  google_fonts: ^6.2.1
  
  # State Management (aligned with core package)

  # Functional Programming

  # Immutability (aligned with core package)

  # Utils
  collection: any

  # Firebase & Database
  
  # Utilities
  html_unescape: ^2.0.0
  app_settings: ^6.1.1
  
  # Localization
  flutter_localization: ^0.3.3
  flutter_localizations:
    sdk: flutter
  get: ^4.7.2
  flutter_web_plugins:
    sdk: flutter
  supabase_flutter: any
  shared_preferences: any
  firebase_core: any
  url_launcher: any
  flutter_staggered_grid_view: any
  skeletonizer: any
  flutter_riverpod: any
  dartz: any
  equatable: any
  riverpod_annotation: any
  uuid: any
  go_router: any
  icons_plus: any
  cloud_firestore: any
  firebase_analytics: any
  device_info_plus: any
  package_info_plus: any
  intl: any
dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting
  custom_lint: ^0.8.0
  riverpod_lint: ^3.0.3

  # Code Generation

  # Testing
  mocktail: ^1.0.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  assets:
    - lib/assets/
    - lib/assets/billing/
    - lib/assets/icons/
    - assets/
